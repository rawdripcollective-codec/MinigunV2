services:
  - type: web
    name: minigunv2-docs
    env: static
    buildCommand: echo "Building static site from repository root"
    staticPublishPath: .
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
services:
  - type: web
    name: minigunv2-docs
    env: static
    buildCommand: mkdir -p public && cp index.html public/index.html && echo "Building static site into public/"
    staticPublishPath: public
    routes:
      - type: rewrite
        source: /*
        destination: /index.html